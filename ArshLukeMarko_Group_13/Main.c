
int main() {

	//displayMenu
	//Menu Selection
	//if statements:
	//Trophies
	//		Displays trophies and allows for user to go back to menu
	//Play Match
	//		SetDifficulty Function (use setDifficulty())
	//		SelectMonster Function (use chooseCharacter())
	//		Determine the move order (use determineOrder())
	//		
	//		display the monster along with it's stats (use displayMonster())
	//		while MonsterHealthcheck is > 0
	//				Choose attack or defend in Menu (corresponding functions for both)
	//				Attack() Function or defend()
	//				displayMonster()
	//		display winner (use displayWinner())
	//		if user input's rematch, run game portion again, otherwise quit program.
	//		
	//				

	return 0;
}